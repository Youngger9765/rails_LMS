<%= form_for @school, :url => { :controller => 'schools', :action => 'update', :id => @school } do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update" %>
<% end %>