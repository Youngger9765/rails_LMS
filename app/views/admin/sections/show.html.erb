<p id="notice"><%= notice %></p>

<%= link_to 'Edit' , edit_admin_school_course_section_path(@admin_school, @admin_course, @admin_section) %> |
<%= link_to 'Back' , admin_school_course_sections_path(@admin_school, @admin_course) %>

<h1><%= @admin_section.name%></h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Content Kind</th>
            <th scope="col">name</th>
            <th scope="col">url</th>
            <th scope="col">show</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
        </tr>
    </thead>
    <tbody>
        <% @content_list.each_with_index do |content, index| %>
            <tr>
                <th scope="row"><%= index %></th>
                <td>
                    <%= content[:kind] %>
                </td>
                <td>
                    <%=content[:name]%>
                </td>
                <td>
                    <%=content[:url]%>
                </td>
                <td>
                    <%= link_to("show", admin_school_course_section_path(@admin_school, @admin_course, ))%>
                </td>
                <td>
                
                </td>
                <td>
                
                </td>
            </tr>
            <%end%>
    </tbody>
    
</table>