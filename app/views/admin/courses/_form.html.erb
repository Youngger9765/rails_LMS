<%= form_with(model: [@admin_school, @admin_course]) do |f| %>
  <% if admin_course.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(admin_course.errors.count, "error" ) %> prohibited this admin_course from being saved:
      </h2>

      <ul>
        <% admin_course.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
          <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card-admin">
    <div class="card-body">
      <div class="mb-3">
        <%= f.label :name, { class: "form-label" }%>
        <%= f.text_field :name, { class: "form-control" } %>
      </div>
      <div class="mb-3">
        <%= f.label :description, { class: "form-label" }%>
        <%= f.text_area :description, { class: "form-control" } %>
      </div>
      <div class="mb-3">
        <%= f.label :goal, { class: "form-label" }%>
        <%= f.text_field :goal, { class: "form-control" } %>
      </div>
      <div class="mb-3">
        <%= f.label :note, { class: "form-label" }%>
        <%= f.text_field :note, { class: "form-control" } %>
      </div>
      <div class="mb-3">
        <%= f.label :tool, { class: "form-label" } %>
        <%= f.text_field :tool, { class: "form-control" } %>
      </div>
      <%= f.submit 'Submit' , :class=> "btn btn-primary"%>
    </div>
  </div>
<% end %>