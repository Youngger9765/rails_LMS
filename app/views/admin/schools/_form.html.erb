<%= form_with(model: admin_school) do |form| %>
  <% if admin_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_school.errors.count, "error") %> prohibited this admin_school from being saved:</h2>

      <ul>
        <% admin_school.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
