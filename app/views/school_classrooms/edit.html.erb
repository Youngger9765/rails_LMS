<%= form_for @classroom, :url => school_classroom_path(@school, @classroom), :html => { :method => :patch } do |f| %>
   <%= render :partial => 'form', :locals => { :f => f } %>
   <%= f.submit %>
<% end %>